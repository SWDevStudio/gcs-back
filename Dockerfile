#Используем какой то образ в данном случае node если нужно указать версию node:x.x.x
FROM node:14.18.2

#Рабочая деректория образа (не понятно что значит)
WORKDIR ${WORKDIR}

## 1й аргумент откуда копировать (. корень) 2й куда копировать
COPY . .

#Выполнить команду от CMD отличается тем, что запускается при сборке образа
RUN yarn install

ENV PORT=3000

#По какому порту запускать приложение
EXPOSE $PORT

# Выполнить команду node index.js при включении контейнера
#CMD ["yarn", "start"]
